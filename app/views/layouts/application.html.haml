!!!
%html
  %head
    %title Iou
    = stylesheet_link_tag    "application"
    = stylesheet_link_tag    "http://fonts.googleapis.com/css?family=Droid+Serif"
    = stylesheet_link_tag    "http://fonts.googleapis.com/css?family=Droid+Sans"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %header#top-bar.container
      #account-menu
        - if user_signed_in?
          = current_user.email
          ·
          = link_to 'Sign out', :destroy_user_session, :method => :delete
        - else
          =link_to 'Sign in', :new_user_session
      %nav
        %ul
          %li= link_to 'My Balance', dashboard_path
          %li= link_to 'All Bills', transactions_path
          %li= link_to 'Create A New Bill', new_transaction_path, class: "btn btn-new"
    %section#content.container
      - if notice
        .flash
          %p{:class => :notice}= notice
      - if alert
        .flash
          %p{:class => :notice}= alert

      = yield
